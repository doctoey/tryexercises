function add(a, b) {
    return a + b;
}

function fizzBuzz(num) {
    if (input % 3 === 0) {
        return 'Fizz'
    }

    return `${num}`
}

//การบ้าน
//convert ตัวเลข -> ภาษาไทย
// 1 -> หนึ่ง
// 10 -> สิบ
// 11 -> สิบเอ็ด
// 21 -> ยี่สิบเอ็ด
// 100 -> หนึ่งร้อย
// 1000

function convert(num) {

}

module.exports = {
    add,
    fizzBuzz
}